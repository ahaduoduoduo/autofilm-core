# AutoFilm Core development status

Updated: 2026-08-15

## 已完成

- [x] 2026-08-15：完整字幕大陆用词转换使用 10 分钟专用 AI 请求超时，不再被通用
  120 秒时限中止；其他主 Agent、会话压缩和 AI 请求继续使用 120 秒默认值。
- [x] 2026-08-15：字幕大陆用词转换提示词升级到 v2；删除对请求隔离方式的
  冗余说明，重点识别香港字幕中的粤语口语直写和半文半白翻译腔，以及港台
  字幕中不符合中国大陆习惯的用词、名词、译名、语序和遣词造句。
- [x] 2026-08-15：修复超大会话首次压缩串行执行且失败后从头处理的问题；多分块摘要
  最多 3 个并发，每个成功分块立即保存，后续只处理失败或未开始的分块。全部完成后
  统一生成唯一滚动检查点并原子删除临时草稿，主 Agent 不读取草稿；单分块的常规滚动
  压缩仍保持一次模型调用。
- [x] 2026-08-15：将已绑定成员的 Native Agent 请求改为 `202` 快速接收、SQLite
  持久化后台执行和 Outbox 最终回复，消除 Adapter 180 秒超时与长时间模型重试、字幕
  处理之间的冲突；事件继续按 `event_id` 去重，同一会话保持顺序。Core 重启时不自动
  重放执行中的工具请求，而是记录失败并通知成员重新发送，避免重复产生副作用。
- [x] 2026-08-14：增加观看后主动处理现有 OpenList 外挂字幕的通用工作区流程；同一
  工作区可导入多集字幕，每个文件使用一次完整、独立的 AI 上下文进行大陆用词转换，
  仅允许修改中文汉字段，输出为新的 `chs` 字幕并保留原字幕。下载字幕原有的放置前
  自动去广告行为保持不变，广告清理、用词转换和 ASS 样式复用统一处理器。
- [x] 2026-08-14：将主 Agent 上下文改为 Pi 式单层滚动检查点；按 Token 保留近期
  原始消息，删除固定条数截断、影视主题摘要、主题工具和额外用户原话副本。系统提示词、
  当前时间和成员长期记忆继续在每次主模型调用时重新注入。
- [x] 2026-08-14：统一 AI 客户端增加临时错误自动重试；服务过载、处理失败、超时、
  网络错误、普通限流和可恢复 5xx 最多重试 3 次，支持 `Retry-After` 和带抖动的指数
  退避，永久鉴权、计费及配额错误不重试。
- [x] 2026-08-14：固定 GitHub Actions 构建与 GHCR 部署流程；本机只运行类型检查和
  直接测试，禁止生成本地发布产物或 Docker 镜像。生产数据库迁移前必须生成一致性
  备份，新镜像健康后清理本次产生的本地产物和被替换旧镜像。
- [x] 2026-08-09：将 Backrest、Btrfs 快照、DSM/Docker 恢复资料和备份调度迁移到
  独立 `ahaduoduoduo/nas-backup` 项目；Core 仅继续运行共享的 OpenList 存储接口。
- [x] 2026-08-09：将 `/volume1/docker` 与 `/volume1/web` 改为完整根目录备份；新增
  项目自动进入备份，不再按服务维护独立路径和前端开关。
- [x] 2026-08-09：将 Home Assistant Recorder 历史与 Backrest 操作记录加入 SQLite
  一致性导出；缩小 Jellyfin 在线数据库排除范围，保留历史数据库副本，并排除 macOS、
  DSM 和 Windows 生成的索引、资源分叉、缩略图与回收站目录。
- [x] 2026-08-09：排除 Live Proxy 源码目录中的旧数据副本，频道运行数据只从
  `/volume1/docker/live-proxy` 上传一次。
- [x] 2026-08-09：部署 AutoFilm Core、Backrest、WeClaw、Telegram Adapter、Home
  Assistant 和 Live Proxy 的 `/volume1/docker` 一级服务目录；Telegram 改用普通 bind
  mount，Home Assistant 改由可复现的 Compose 管理，恢复资料已按新路径重新生成。
- [x] 2026-08-09：部署 Backrest 手机错误通知修复和失败后恢复状态；年度日历在重试
  成功后显示正常，任务条以绿色加橙色外圈记录已恢复，并使现有与新增任务排除
  Finder 生成的 `.DS_Store`。
- [x] 2026-08-09：将 Backrest 的 Restic 元数据缓存持久化到独立主机目录，避免容器
  更新后重复下载全部历史索引；备份任务卡片增加失败后可再次执行的“立即备份”入口。
- [x] 2026-08-09：固定 OpenList 重新认证镜像；扫码成功后使用新客户端验证登录和
  上传身份再切换会话，并将无效 Cookie 状态识别为需要重新扫码。
- [x] 2026-08-09：固定包含 Docker 命名卷、DSM 套件配置和数据库一致副本说明的
  Backrest 提交镜像，并用于生产备份界面。
- [x] 2026-08-09：为每次 Restic 备份增加主机请求与等待机制；原子生成 DSM 原生配置、
  AI 可读系统重建资料、完整 Docker 运行清单、渲染与生成 Compose，以及 OpenList、
  AutoFilm、Jellyfin、Subhub、LocalProxy、NAS Gateway Manager、AutoAccount 的一致
  SQLite 副本；补充 Telegram 命名卷与 `/volume1/web/live` 数据范围。
- [x] 2026-08-09：补充 `/volume1/@appconf` 与 `/volume1/@appdata` 的套件配置，排除
  VMM 活动 etcd 和套件日志；记录 VMM 虚拟机、存储池和 DSM 特殊存储容量供重建核对。
- [x] 2026-08-09：发现新的 115 HTTP 405 风控或凭据缺失状态时只创建一个扫码会话，将同一张
  二维码发送到全部管理员微信和 Telegram 身份；Telegram Adapter 改为读取 Core
  内部图片后通过 multipart 上传。
- [x] 2026-08-09：自动二维码发送后继续读取有效会话状态，扫码确认时由 OpenList
  使用原客户端类型更新并保存 Cookie。

- [x] 2026-08-08：部署 OpenList 115 上传初始化响应解密重试修复；GitHub 单架构
  构建和实际增量备份验证通过，异常响应由 OpenList 内部重试后返回 HTTP 200。
- [x] 2026-08-06：按参考设计重构 Backrest 信息层级：使用大号容量数字、分隔式指标、
  编号内容目录和背景曲线；修正移动设备 viewport，手机导航改为完整页面，并增加
  iPhone 13 浏览器上下文下的首页、导航与全屏编辑器 E2E 验证。
- [x] 2026-08-06：重新设计 Backrest 手机首页的信息顺序，将“备份内容”放在首屏，
  增加计划直接编辑入口，并使计划编辑器完整覆盖 390×844 手机视口。
- [x] 2026-08-06：更新 Backrest 移动端布局；首页直接显示每个计划的备份内容与
  排除内容，计划编辑器使用中文内容开关并保留高级 Restic 路径规则。
- [x] 2026-08-06：部署 OpenList 原生 Restic 网关和定制 Backrest；GitHub Actions
  仅构建群晖使用的 linux/amd64 镜像，生产配置包含 Docker、Home Assistant、DSM
  证书和独立 Time Machine 恢复计划，并记录流量限制与跨 NAS 恢复方式。
- [x] 2026-08-05：增加第一版本地上下文压缩；该多层方案已于 2026-08-14 被单层滚动
  检查点替代，完整原始消息继续保存在 SQLite。

- [x] 2026-08-01：资源升级在原 Jellyfin Item 替换完成后恢复发起任务的 Agent 会话，
  继续已经确定的字幕等操作；每个升级项独立恢复，不等待同批任务。新任务取消重复的
  固定结果通知，历史任务保留兼容通知；主 Agent 默认提示升级到版本 22。
- [x] 2026-08-01：升级候选改用与升级子任务绑定的 `upgrade_selection_id`，从工具
  参数结构上隔离普通 Jackett 搜索候选；升级结果不再返回完整 Jellyfin 轨道，提交 ID
  位于紧凑结果中并受统一 Token 预算保护；旧未完成任务可重新取得新选择 ID。
- [x] 2026-08-01：主 Agent 正常提交后只简短报告已发生的事实，不主动解释 OpenList
  与 115 的内部阶段，也不列举尚未发生或不会执行的事项；下载工具等待 115 明确接受
  或失败后才返回，默认提示升级到版本 19。
- [x] 2026-07-31：离线任务区分 OpenList 本地排队与 115 provider 已接受状态；
  40 秒从 provider 返回任务标识后开始计算，并保留每次尝试的提交审计信息；主 Agent
  默认提示升级到版本 17，未自定义配置自动取得新的状态说明规则。
- [x] 2026-07-31：增加最多 1000 个电影版本的后台画质升级检查；Core 构造搜索词并
  以 8 项并发查询 Jackett，只把目标分辨率命中项分页交给 Agent，未命中条目不进入
  模型上下文，完成后恢复原聊天发送结果通知。
- [x] 2026-07-31：增加按成员隔离的长期记忆；仅在明确要求时新增、修改或删除，
  每轮自动加入当前成员系统上下文，且 `/new`、`/clear` 不删除长期记忆。
- [x] 2026-07-31：修复资源升级重命名后立即移动时的远端对象暂不可见问题；支持继续
  旧版本遗留的唯一中间文件，并在下载前、实际文件识别后拒绝相同发布和相同大小资源。
- [x] 2026-07-31：资源替换专用 ffprobe 对临时远端读取失败执行最多 3 次有限重试；
  增加 BoxSet 成员媒体详情工具，并从电影数量和未知分辨率统计中排除虚拟合集。
- [x] 2026-07-31：资源升级在历史 Jellyfin 路径不存在时，只检查直接父目录中的
  唯一分隔符等价目录和文件；扩展名必须一致且文件大小误差不超过 1 MiB。解析出的
  真实旧路径同时用于新文件放置、旧文件备份和 Jellyfin 回滚，不执行全盘查询。
- [x] 2026-07-31：增加现有 OpenList Movie/Episode 资源升级；多个目标使用稳定
  子任务和候选 ID 并发查询与下载，每项完成后独立处理，不等待同批其他项目。
- [x] 2026-07-31：Jackett HTTP torrent 在 Core 本地解析为 v1 magnet；离线接口
  只接受已验证 magnet，资源通知使用 Jackett 标题且不暴露内网 URL/API Key。
- [x] 2026-07-31：资源升级下载使用独立临时目录且不执行普通 Jellyfin 刷新；
  Jellyfin 原位更新 Item 路径和媒体流，保留用户数据、Provider ID、图片及元数据。
- [x] 2026-07-31：替换前使用 Jellyfin 命名规则识别视频并执行实际 ffprobe；
  实测分辨率低于旧文件时拒绝替换，成功后旧文件移动到独立备份目录。
- [x] 2026-07-31：升级状态保存到 SQLite，服务重启后继续未完成项目；一项失败不影响
  其他项目，支持明确确认后把备份旧文件恢复到同一个 Jellyfin Item ID。
- [x] 2026-07-31：增加 Jellyfin 电影分辨率分页清单；使用实际媒体流识别裁切后的
  720p、1080p、1440p、2160p，未知宽高不请求 OpenList 重新探测。
- [x] 2026-07-31：增加重复电影分析；TMDB/IMDb ID 相同标记为确定重复，仅标题
  年份相同标记为疑似重复，返回每个实际版本供 Agent 核对后再请求删除。
- [x] 2026-07-31：增加 TMDB 电影、剧集整体、单季和单集的统一详情工具，返回
  评分人数、剧情简介和播出日期，中文简介缺失时回退英文。
- [x] 2026-07-31：模型请求动态加入服务器时间；时间敏感回答要求再次调用时间工具。
- [x] 2026-07-31：曾增加按 TMDB 身份管理的影视主题摘要；该机制及数据库表已于
  2026-08-14 删除，由唯一滚动检查点负责较早历史。
- [x] 2026-07-31：字幕放置改为最多 8 项并发，文本字幕的独立 AI 清理与所有格式的
  流式上传均可并行；修复 `Penguin` 中 `eng` 子串导致中文字幕被标记为英语的问题。
- [x] 2026-07-30：增加 Jellyfin Movie/Episode 精确版本删除工具；删除前核对
  条目类型和实际路径，支持重复分集及电影旧版本批量部分成功，并继续通过摘要引用
  删除外挂字幕。
- [x] 2026-07-30：所有字幕格式改用 AutoFilm Jellyfin 鉴权二进制流式上传，
  删除 Core 的 Base64 JSON 上传，不再因大型 SUP/PGS 触发 30 MB 请求限制。
- [x] 2026-07-30：大型 SUP/PGS 从 workspace 文件直接创建读取流，不再整体载入
  Core 内存；文本字幕清理后同样使用流式请求，并在失败结果中返回具体执行阶段及底层原因。
- [x] 2026-07-30：同一批分集字幕强制复用单个 workspace、并行下载并批量映射放置；
  工具轮次达到边界时返回真实阶段性结果，不再向聊天渠道暴露内部迭代异常。
- [x] 2026-07-30：曾按固定消息数量保留完整用户请求；固定数量入口已于 2026-08-14
  删除。数据库插入顺序、孤立工具结果清理和中断结果恢复继续保留。
- [x] 2026-07-30：115 秒传时限改为默认 40 秒；超时后删除当前任务并等待成员
  明确选择备用资源，不再自动下载备用磁力。
- [x] 2026-07-30：备用资源提示由进度 Worker 主动发送时，Agent 根据 waiting
  任务元数据解释成员后续回复，不依赖该提示出现在聊天模型历史中。
- [x] 2026-07-30：离线任务成功通知延迟到 Jellyfin 精确导入之后；恢复原 Agent
  会话执行已经约定的可选字幕处理，并允许没有字幕计划时直接完成。
- [x] 2026-07-30：离线任务完成后使用 OpenList 返回的最终结果路径精确刷新
  Jellyfin，并拒绝目标目录以外的结果路径。
- [x] 2026-07-30：微信主动通知缺少当前会话令牌时保留在 Outbox，不消耗失败
  次数；用户下一次发送消息后继续投递。
- [x] 2026-07-30：电影任务保存 TMDB ID，并显式要求 Jellyfin 将该 ID 绑定到
  精确结果目录中的单个视频，而不是月份父目录。
- [x] 建立独立 `ahaduoduoduo/autofilm-core` 仓库和 npm workspace。
- [x] 实现 SQLite 数据库、首次所有者初始化、会话认证和角色模型。
- [x] 实现 AI 供应方/协议分离和四种协议 Adapter。
- [x] 将 Responses 设为默认新协议，同时保留 Chat Completions。
- [x] 实现 TMDB、Jackett、OpenList、Jellyfin 基础 Agent 工具。
- [x] 实现 Native Message Service 入站认证、事件去重和成员默认拒绝。
- [x] 实现离线任务进度读取、持久化和带重试的聊天通知 Outbox。
- [x] 实现 OpenList 115 扫码登录界面和管理员聊天二维码发送工具。
- [x] 实现 AI、成员、身份、渠道、媒体服务、任务和 Agent 测试界面。
- [x] 提供 Core 单服务 Compose 和相邻源码完整 Compose。
- [x] 补充协议、架构、安全、部署和媒体交互文档。
- [x] 添加认证、协议 Adapter、Native 事件和任务进度测试。
- [x] 2026-07-28：完成 Docker 运行验证，修正 OpenList 受限令牌路径、
  Jellyfin 12 标准鉴权、TMDB v3 API Key 和 WeClaw 前台运行配置。
- [x] 2026-07-29：迁移批量下载、OpenList 视频列举、SubHD 搜索/详情/下载、
  字幕解压/清理/放置、ASS 样式、Jellyfin 图片/分集/媒体流/条目刷新工具。
- [x] 2026-07-29：验证码改为独立视觉模型上下文，最多自动识别五次；人工处理
  只使用进程内一次性状态和临时文件，不写数据库。
- [x] 2026-07-29：建立按成员隔离的追更表、只读检查 Agent、定时任务和管理页面。
- [x] 2026-07-29：补充 OpenAI 两种协议的多模态映射、ASS、临时字幕工作区和
  旧数据库迁移、完整工具注册测试，后端共 17 项测试通过。
- [x] 2026-07-29：依据旧 AutoFilm 源码重做 Core 管理界面，恢复中性黑白灰主题、
  顶部横向标签导航、350px 登录卡片、Geist 字体和旧版基础组件规格。
- [x] 2026-07-29：管理界面改为固定深色，删除主题切换，恢复旧版标签滑块、页面横向
  切换、弹窗和通知动画，并加强深色卡片的边框与阴影层次。
- [x] 2026-07-29：曾增加固定数量 Agent 历史；该上下文入口已于 2026-08-14 删除。
  同期的 115 秒传判定、远端任务删除、备用候选和尝试历史继续保留。
- [x] 2026-07-29：增加独立 Telegram Adapter 镜像、long polling、群组 Topic、
  双向消息、管理界面模板和 Compose 独立服务。
- [x] 2026-07-29：简化 Telegram 配置为仅填写 BotFather Token；Adapter 默认以
  未配置状态启动，内部完成 Bot 验证、双向令牌生成和独立数据卷保存。
- [x] 2026-07-29：同一 Compose 内自动识别 WeClaw 登录账号和双向令牌；微信管理
  页面删除 Bot ID、容器地址、令牌及 JSON 配置输入，仅保留状态与启用开关。
- [x] 2026-07-29：OpenAI Responses 客户端改用标准 SSE 流式请求，兼容强制
  `stream: true` 的供应方，并解析文本、用量和工具调用事件。
- [x] 2026-07-29：迁移旧版可继续适用的 Agent 搜索、资源与字幕评估、批量下载、
  字幕处理、图片、媒体流、主动查询和追更行为；删除 Telegram HTML/按钮、
  本地软链接、目录同步和本地文件删除等旧规则。
- [x] 2026-07-29：将主 Agent、验证码 OCR、字幕广告清理和追更判断提示词保存到
  SQLite，增加管理页面编辑、恢复默认值和默认版本升级保护。
- [x] 2026-07-29：把 Agent 文本中的 Core 临时媒体 URL 转换为 Native 图片消息；
  WeClaw 与 Telegram Adapter 直接发送实际图片。
- [x] 2026-07-29：离线任务完成后由 Worker 按目标目录合并 Jellyfin 刷新请求，
  失败状态保存在任务元数据并按退避间隔重试。
- [x] 2026-07-29：恢复 Jackett 全量结果按大小降序和 20 条分页，增加同查询缓存；
  同轮资源、字幕等工具改为并行执行。
- [x] 2026-07-29：恢复下载目录的代码级保护；Core 根据 TMDB ID、媒体类型和季号
  自动生成电影、单季与多季合集目录，下载工具不再接受模型填写的目标目录。
- [x] 2026-07-29：电视剧下载完成后统一刷新剧集根目录并携带 TMDB ID，增加
  单季、多季合集、参数校验和下载工具测试。
- [x] 2026-07-30：SubHD 搜索恢复关联影片页完整列表、评分、评论数量和详情回复。
- [x] 2026-07-30：字幕任务改为成员级多包 workspace，保留完整解压相对目录、
  集号和语言提示，并支持多个包一次性批量配对。
- [x] 2026-07-30：字幕目标改用 Jellyfin Movie/Episode ID；本地与 OpenList
  字幕新增、替换、删除统一使用 Jellyfin 字幕管理接口，不再直接上传后刷新目录。
- [x] 2026-07-30：恢复逐字幕独立 AI 全量广告判断、旧版 ASS 黑边处理、
  7z/unzip/unrar 回退及 GB18030/GBK/GB2312 编码归一化。
- [x] 2026-07-30：批量字幕处理改为单项失败后继续执行，失败时保留 workspace；
  人工验证码按成员、workspace 和下载请求隔离，不阻止其他下载。
- [x] 2026-07-30：删除字幕工作区数字序号，文件与 Agent 工具协议统一使用不可变
  UUID；增加文件摘要、重复配对保护和不可变批量放置计划。
- [x] 2026-07-30：Jellyfin 外挂字幕替换与删除改用摘要引用；执行前重新读取条目，
  字幕流位置变化时解析当前目标，文件变化时拒绝操作。
- [x] 2026-07-30：SubHD 下载改为每任务独立 session 与 Cookie Jar；请求开始遵守
  统一间隔但允许网络响应、验证码识别和解压并发执行，人工验证码使用独立任务码。
- [x] 2026-07-30：完成两个真实 SubHD 字幕包的并发下载验证；本地网络夹具验证慢响应
  确实重叠、两个 Cookie Jar 不混用且并发验证码提交返回各自文件。
- [x] 2026-07-30：改为读取 OpenList 被动记录的 115 HTTP 405 标记，不再定时访问
  115；向所有已绑定管理员渠道发送一次通知，扫码或真实请求恢复后清除标记。
- [x] 2026-07-30：Telegram 按会话串行、跨会话并发处理消息，持久化 Update offset，
  并统一支持 `/new`、`/clear` 和 `/reset`。
- [x] 2026-07-30：删除 Core 未使用的 OpenList 文件客户端、无效渠道类型、重复刷新
  模式与审计空表；增加过期会话、事件去重和已完成 Outbox 定期清理。
- [x] 2026-07-30：完成群晖现有环境接管验证；OpenList 使用旧 AList 数据目录，
  Jellyfin v12 使用原配置、缓存和媒体目录，Core、OpenList、Jellyfin 均通过
  原有反向代理访问。
- [x] 2026-07-30：执行 Jellyfin 旧路径迁移并验证再次预检无待处理项；验证远端
  视频 302、文本字幕延迟迁移和 SUP 本地直出。
- [x] 2026-07-30：将六个源码仓库整理到统一工作目录；补充上游关系、修改边界、
  服务通信、交互流程、Compose、构建、部署和本机运行文档。
- [x] 2026-07-30：建立 GitHub Actions 中央镜像构建；生产 Compose 默认拉取
  GHCR。历史本地覆盖文件已于 2026-08-14 退出当前发布流程。
- [x] 2026-07-30：删除已停用的旧 AList、Jellyfin、Nginx 容器和镜像，保留当前
  OpenList/Jellyfin 继续使用的持久化目录。
- [x] 2026-07-30：修复 115 进度达到 100% 但尚未结束时过早通知和刷新 Jellyfin；
  完成判定使用 OpenList `StateSucceeded`，并禁止终态任务被旧快照改回运行状态。
- [x] 2026-07-30：同一成员会话的顶层 Agent 请求改为按顺序执行，避免并发请求读取
  尚未写入工具输出的中间历史；不同会话及单次请求内工具继续并行。
- [x] 2026-07-30：TMDB 作品身份唯一确定后由 Core 获取封面，Native Adapter
  先发送封面图片再发送 Agent 文字，不依赖模型复制外部图片地址。

## 未完成（记录于 2026-07-31）

- [ ] 增加追更暂停、恢复和手动立即检查操作。
- [ ] 使用真实的需验证码 SubHD 下载分别验证四种视觉协议；自动测试当前使用本地夹具。
- [ ] 增加 Playwright 前端端到端测试。
- [ ] 补充 ESLint 依赖和共享配置，使现有 `npm run lint` 脚本可执行。
- [ ] 增加资源升级备份的保留期限和管理员清理策略；当前备份不会自动删除。
- [ ] 在现有 GHCR `linux/amd64` 工作流稳定后增加 arm64 镜像。
- [ ] 明确公开发布许可证。

## 已明确删除的旧职责

- [x] 软链接创建和维护。
- [x] rclone 挂载与重新挂载。
- [x] Nginx 视频和 SUP 字幕改写。
- [x] 定时目录同步与精准扫描插件调用。
- [x] 直接操作本地文件系统的媒体目录删除工具；当前删除统一经过 Jellyfin 权限、
  条目和远端文件处理。
- [x] Codex 登录作为 AI 供应方。
